import styled from 'styled-components';
import { Tag, Input } from 'antd';

export const DashTag = styled(Tag)`
  background: #fff;
  border-style: dashed;
`;

export const InputTag = styled(Input)`
  width: 78px;
  height: 23px;
  margin-right: 8px;
  vertical-align: top;
`;
